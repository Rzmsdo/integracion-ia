import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Document Analyzer - Análisis Inteligente con IA",
  description: "Analiza documentos (PDF, DOCX, TXT, MD) con inteligencia artificial usando Ollama. Obtén resúmenes, puntos clave, análisis de sentimiento y más.",
  keywords: ["document analysis", "AI", "Ollama", "PDF analyzer", "text analysis"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
